# Web Framework
fastapi==0.110.0
uvicorn==0.27.1        # The ASGI server that actually runs FastAPI
python-multipart==0.0.9 # Required by FastAPI to parse file uploads (PDFs)

# Environment Validation
pydantic==2.6.3
pydantic-settings==2.2.1

# Background Worker & Broker Integration
celery==5.3.6
redis==5.0.1

# Document Processing & ML Basics (Placeholders for Phase 2)
PyMuPDF==1.23.21       # Extremely fast PDF text extraction